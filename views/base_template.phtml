<!DOCTYPE html>
<html>
	<head>
		<base href="/">
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="description" content="">
        <meta name="keywords" content="">
		<meta name="author" content="OneTimeCZ">
		<title>DofE | <?= $data["title"] ?></title>
		<script src="includes/scripts/jquery-1.11.3.js"></script>
		<script src="includes/scripts/bootstrap.min.js"></script>
        <script src="includes/scripts/searchbar.js"></script>
        <?php if(isset($data["js"])): ?>
            <?php $scripts = is_array($data["js"]) ? $data["js"] : array($data["js"]); foreach($scripts as $js): ?>
            <script src="includes/<?= $js ?>.js"></script>
            <?php endforeach; ?>
        <?php endif; ?>
		<link rel="shortcut icon" type="image/png" href="includes/images/favicon.png"/>
        <?php if(isset($data["css"])): ?>
            <?php $styles = is_array($data["css"]) ? $data["css"] : array($data["css"]); foreach($styles as $css): ?>
            <link rel="stylesheet" type="text/css" href="includes/<?= $css ?>.css" />
            <?php endforeach; ?>
        <?php endif; ?>
		<link rel="stylesheet" type="text/css" href="includes/css/bootstrap.min.css" />
        <link rel="stylesheet" type="text/css" href="includes/css/font-awesome.min.css" />
		<link rel="stylesheet" type="text/css" href="includes/css/style.css" />
	</head>
	<body>
        <div class="page-container">
            <header>
                <?php require_once 'views/header.phtml'; ?>
            </header>
            <div class="container-fluid col-lg-offset-1 col-lg-10">
                <div class="main-content">
                    <?php if(!empty($data["popups"])): ?>
                        <?php foreach($data["popups"] as $f): ?>
                            <?php require 'popup.phtml'; ?>
                        <?php endforeach; ?>
                    <?php endif; ?>
                    <aside class="col-sm-4 col-sm-push-8 hidden-xs">
                        <?php require_once 'views/aside.phtml'; ?> 
                    </aside>
                    <div class="main col-sm-8 col-sm-pull-4">
                        <?php require_once 'views/' . $view . '.phtml'; ?>
                    </div> 
                </div>
            </div>
            <footer>
                <?php require_once 'views/footer.phtml'; ?>
            </footer>
        </div>
	</body>
    <?php require_once 'views/modals/login.phtml' ?>
</html>