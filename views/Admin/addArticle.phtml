<div class="page-heading">
    <h1>Přidat nový článek</h1>
</div>

<div class="section">
    <div class="column single">
        <div class="content-box">
            <form role="form" enctype="multipart/form-data" id="new_article" action="/administrace/clanky/ulozit" method="post">
                <div class="col-lg-6">
                    <div class="form-group">
                        <label for="title">Nadpis článku</label>
                        <input class="form-control" type="text" name="title" placeholder="Zadejte nadpis článku" id="title" required>
                        <input type="hidden" name="author" value="<?= $_SESSION["user"]->getId() ?>">
                    </div>
                    <div class="form-group">
                        <label for="keywords">Klíčová slova</label>
                        <input class="form-control" type="text" name="keywords" placeholder="Zadejte klíčová slova článku" id="keywords" required>
                    </div>
                    <div class="form-group">
                        <label for="category">Kategorie</label>
                        <select class="form-control" name="category" id="category">
                            <?php foreach($data["categories"] as $category): ?>
                            <option <?= $category->getName()=="Obecné" ? "selected='selected'" : "" ?> value="<?= $category->getId() ?>"><?= $category->getName() ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>
                
                <div class="col-lg-6">
                    <div class="form-group">
                        <label for="content">Obsah článku</label>
                        <textarea class="form-control" name="content" id="content">
                            
                        </textarea>
                    </div>
                    <div class="form-group">
                        <input class="btn btn-lg btn-default pull-left" type="submit" name="save" value="Přidat" id="add">
                    </div>
                </div>
                
                <div class="col-xs-12">
                    <div class="form-group">
                        <label for="croppbox-input">Fotografie</label>
                        <div id="croppbox"></div>
                        <input class="btn btn-default" name="img" type="file" id="croppbox-input">
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>