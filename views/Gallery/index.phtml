<div class="content">    
    <div class="section">
        <div class="page-heading">
            <h1>Galerie</h1>
        </div>
    </div>
    
    <?php if(!$data["galleries"]->isEmpty()): ?>
    <div class="section">
        <div class="column single">
            <div class="content-box">
                <div class="section-content">
                    <div class="gallery-list">
                        <?php foreach($data["galleries"] as $gallery): ?>
                        <div class="gallery-item col-sm-6 col-lg-4">
                            <div class="inner">
                                <a class="gallery-link" href="/galerie/<?= $gallery->getId() ?>"><?= $gallery->getName() ?></a>
                            </div>
                        </div>
                        <?php endforeach; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <?php endif; ?>
</div>