<?php
    use Models\UserQuery;
    use Models\ImageQuery;
?>
<nav class="navbar navbar-inverse navigation">
    <div class="container-fluid col-md-offset-1 col-md-10">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle menu" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>                        
            </button>
            <a href="/"><img src="../includes/images/logo-dofe-gray.png" title="DofE logo" alt="DofE logo"></a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navigation search visible-xs">
                <li>
                    <div class="input-group search col-md-12">
                        <input type="text" class="  search-query form-control" placeholder="Vyhledávání" />
                        <span class="input-group-btn">
                            <button class="btn btn-default" type="button">
                                <span class=" glyphicon glyphicon-search"></span>
                            </button>
                        </span>
                    </div>
                </li>
            </ul>
            <ul class="nav navbar-nav navigation">
                <li <?php $class = $data['active'] == 'gallery' ? 'class = active' : ''; echo $class; ?> >
                    <a class="border" href="/galerie">GALERIE</a>
                </li>
                <li <?php $class = $data['active'] == 'blog' ? 'class = active' : ''; echo $class; ?> >
                    <a class="border" href="/clanky">BLOG</a>    
                </li>
                <?php if(isset($_SESSION["user"]) && UserQuery::create()->findPk($_SESSION["user"])->getPermissions() > 1): ?>
                <li <?php $class = $data['active'] == 'admin' ? 'class = active' : ''; echo $class; ?> >
                    <a class="border" href="/administrace">ADMINISTRACE</a>    
                </li>
                <?php endif; ?>
            </ul>
            <ul class="nav navbar-nav navigation login-box navbar-right">
                <li <?php $class = $data['active'] == 'profile' ? 'class = active' : ''; echo $class; ?> >
                    <?php if (!isset($_SESSION['user'])): ?>
                        <a class="border" data-toggle="modal" data-target="#login_modal" href="#">PŘIHLÁSIT SE</a>
                    <?php else: ?>
                        <!-- SQL profile picture -->
                        <a class="borderless" href="/profil/<?= UserQuery::create()->findPk($_SESSION["user"])->getUrl() ?>"><p><?= UserQuery::create()->findPk($_SESSION["user"])->getUsername(); ?></p><img alt="profilepic" class="img img-responsive" src="../includes/images/50x50/<?= ImageQuery::create()->findPk(UserQuery::create()->findPk($_SESSION["user"])->getIdImage())->getPath(); ?>"></a>
                    <?php endif; ?>
                </li>        
            </ul>
        </div>
    </div>
</nav>